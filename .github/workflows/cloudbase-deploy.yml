name: Deploy to CloudBase CloudRun

on:
  push:
    branches:
      - dev_tcb  # 推送到 dev_tcb 分支时触发
  workflow_dispatch:  # 允许手动触发

env:
  TCB_ENV_ID: ${{ secrets.TCB_ENV_ID }}  # 腾讯云环境ID
  TCB_SECRET_ID: ${{ secrets.TCB_SECRET_ID }}  # 腾讯云 SecretId
  TCB_SECRET_KEY: ${{ secrets.TCB_SECRET_KEY }}  # 腾讯云 SecretKey

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install CloudBase CLI
        run: npm install -g @cloudbase/cli

      - name: Login to CloudBase
        run: |
          tcb login --apiKeyId ${{ secrets.TCB_SECRET_ID }} --apiKey ${{ secrets.TCB_SECRET_KEY }}

      - name: Verify environment
        run: |
          echo "🔍 验证环境配置..."
          echo "Environment ID: ${{ secrets.TCB_ENV_ID }}"
          echo "Service Name: ${{ secrets.TCB_SERVICE_NAME || 'budget-manager' }}"
          tcb env:list || echo "⚠️ 无法列出环境，但继续部署..."

      - name: Deploy to CloudRun
        run: |
          echo "🚀 开始部署..."
          SERVICE_NAME="${{ secrets.TCB_SERVICE_NAME || 'budget-manager' }}"
          # 确保服务名称符合规范（只包含小写字母、数字、连字符）
          SERVICE_NAME=$(echo "$SERVICE_NAME" | tr '[:upper:]' '[:lower:]' | sed 's/[^a-z0-9-]/-/g')
          # 移除开头和结尾的连字符
          SERVICE_NAME=$(echo "$SERVICE_NAME" | sed 's/^-\+//;s/-\+$//')
          echo "📦 使用服务名称: $SERVICE_NAME"
          echo "📋 环境ID: ${{ secrets.TCB_ENV_ID }}"
          echo ""
          echo "⚠️  注意：存储挂载需要在云托管控制台手动配置"
          echo "   1. 进入服务详情 → 存储挂载"
          echo "   2. 选择「云开发对象存储」"
          echo "   3. 挂载到实例目录：/mnt"
          echo "   配置后，每次部署都会保留存储挂载配置"
          echo ""
          echo "y" | tcb cloudrun deploy \
            --envId ${{ secrets.TCB_ENV_ID }} \
            --serviceName "$SERVICE_NAME"
        continue-on-error: false
      
      - name: Verify deployment
        run: |
          echo "✅ 部署完成！"
          echo ""
          echo "📝 后续步骤："
          echo "1. 进入云托管控制台 → 服务详情 → 存储挂载"
          echo "2. 配置存储挂载（如果尚未配置）："
          echo "   - 存储类型：云开发对象存储"
          echo "   - 对象存储挂载目录：/"
          echo "   - 挂载到实例目录：/mnt"
          echo "3. 应用会自动检测并使用 /mnt 目录存储数据库"

